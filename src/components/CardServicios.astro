---
import { Image } from "astro:assets";

interface Props {
  title: string;
  description?: string;
  image: ImageMetadata;
  imageHover: ImageMetadata;
}

const { title, description, image, imageHover } = Astro.props;
---

<a
  href={`https://wa.me/9841085234?text=Hola,%20quiero%20información%20sobre%20${encodeURIComponent(title)}`}
  target="_blank"
  rel="noopener noreferrer"
  class="group transition cursor-pointer duration-300 ease-in-out bg-white relative flex flex-col items-center justify-evenly text-center rounded-3xl hover:scale-105 hover:shadow h-60 w-40 p-5 lg:w-55 lg:h-70"
>
  <Image
    src={image}
    alt={title}
    class="w-15 lg:w-20 absolute top-5 object-cover opacity-100 group-hover:opacity-0"
  />

  <Image
    src={imageHover}
    alt={title}
    class="w-15 lg:w-20 absolute top-5  object-cover opacity-0 group-hover:opacity-100"
  />

  <h3 class="uppercase font-bold mt-20">{title}</h3>
  <p>{description}</p>
</a>
