---
import { Image } from "astro:assets";

interface Props {
  title: string;
  description?: string;
  image: ImageMetadata;
  imageHover: ImageMetadata;
}

const { title, description, image, imageHover } = Astro.props;
---

<a
  href="#"
  class="group transition cursor-pointer duration-300 ease-in-out bg-white relative flex flex-col items-center justify-evenly text-center rounded-3xl hover:scale-105 hover:shadow h-60 w-50"
>
  <Image
    src={image}
    alt={title}
    class="w-20 absolute top-5 object-cover opacity-100 group-hover:opacity-0"
  />

  <Image
    src={imageHover}
    alt={title}
    class="w-20 absolute top-5  object-cover opacity-0 group-hover:opacity-100"
  />

  <h3 class="uppercase font-bold mt-20">{title}</h3>
  <p>{description}</p>
</a>
