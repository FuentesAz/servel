---
type Variant = "primary" | "secondary";

interface Props {
    title: string;
    href: string;
    variant: Variant;
    target?: string;
}

const { title, href, variant, target } = Astro.props as Props;

//---------------------
// Base styles
//---------------------

const baseClasses =
    "py-3 transition-colors cursor-pointer uppercase text-xs xl:text-lg md:text-base rounded-sm font-bold";

//---------------------
// Variant styles
//---------------------

const variantClasses: Record<Variant, string> = {
    primary: "bg-red-700 hover:bg-red-800 text-white font-bold px-[38px]",
    secondary:
        "border-2 border-white hover:bg-white hover:text-black text-white px-[85px] lg:px-[80px] xl:px-[103px]",
};
---

<a rel="noopener noreferrer" target={target} href={href} class={`${baseClasses} ${variantClasses[variant]}`}>{title}</a>

